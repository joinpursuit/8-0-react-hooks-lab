import React from "react";
import "./NavBar.css";
//erase the class
//turn navbar into a function
//remove render because it's a functional component

const NavBar = () => {
    return (
      <nav>
        <h1>Northwest Animal Hospital</h1>
        <h2>New Client Sign-up</h2>
      </nav>
    );
  
}

export default NavBar;
